<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script src="https://kit.fontawesome.com/288e046e53.js" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="bg-dark text-white" >
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <h5>
            <%= link_to "Bookers", "/", class: "text-white" %>
          </h5>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
          <div class="navbar-collapse collapse show">
           <ul class="navbar-nav ml-auto">
             <% if user_signed_in? %>
                <li class="nav-item">
                  <%= link_to user_path(current_user.id), class: "text-white" do %>
                    <i class="fas fa-home"></i><span class="link-text">Home</span>
                  <% end %>
                </li>

                <li class="nav-item">
                  <%= link_to users_path, class: "text-white" do %>
                    <i class="fas fa-users"></i><span class="link-text">Users</span>
                  <% end %>
                </li>

                <li class="nav-item">
                  <%= link_to books_path, class: "text-white" do %>
                    <i class="fas fa-book-open"></i><span class="link-text">books</span>
                  <% end %>
                </li>

                <li class="nav-item">
                  <%= link_to destroy_user_session_path, class: "text-white", method: :delete do %>
                    <i class="fas fa-sign-out-alt"></i><span class="link-text">Log out</span>
                  <% end %>
                </li>
             <% else %>

                <li class="nav-item">
                  <%= link_to root_path, class: "text-white" do %>
                    <i class="fas fa-home"></i><span class="link-text">Home</span>
                  <% end %>
                </li>

                <li class="nav-item">
                  <%= link_to about_path, class: "text-white" do %>
                    <i class="fas fa-link"></i><span class="link-text">About</span>
                  <% end %>
                </li>

                <li class="nav-item">
                  <%= link_to new_user_registration_path, class: "text-white" do %>
                    <i class="fas fa-user-plus"></i><span class="link-text">Sign up</span>
                  <% end %>
                </li>

                <li class="nav-item">
                  <%= link_to new_user_session_path, class: "text-white" do %>
                    <i class="fas fa-sign-in-alt"></i><span class="link-text">Log in</span>
                  <% end %>
                </li>
             <% end %>
           </ul>
          </div>
        </div>
      </nav>
    </header>

    <main>
     <div class="container">
      <div class="row">

         <%= flash[:notice] %>
         <%= yield %>
      </div>
     </div>
    </main>

    <footer>
      <div class="container">
        <div class="row">
          <div class="mx-auto">
           <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
